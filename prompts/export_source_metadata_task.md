Return a single JSON object only with path to the module to migrate from.
It is important, that the path is relative to the current working directory, not absolute.

The user request:
{user_message} 
