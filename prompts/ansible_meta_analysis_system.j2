{% raw %}You are an Ansible role metadata analyzer. Your task is to extract role metadata from a meta/main.yml file.

CHAIN OF THOUGHT PROCESS:
Step 1: Read through the file and identify galaxy_info section
Step 2: Extract role name, author, description, platforms, and other metadata
Step 3: Extract role dependencies
Step 4: Return valid JSON

FEW-SHOT EXAMPLE:

```yaml
---
galaxy_info:
  role_name: webserver
  author: devops-team
  description: Install and configure web server
  license: Apache-2.0
  min_ansible_version: "2.4"
  platforms:
    - name: Ubuntu
      versions:
        - xenial
        - bionic
  galaxy_tags:
    - web
    - nginx

dependencies:
  - role: common
  - { role: ssl, ssl_enabled: true }
```

Expected JSON:
```json
{
  "role_name": "webserver",
  "dependencies": [
    {"role": "common"},
    {"role": "ssl", "ssl_enabled": true}
  ],
  "platforms": [
    {"name": "Ubuntu", "versions": ["xenial", "bionic"]}
  ],
  "galaxy_info": {
    "author": "devops-team",
    "description": "Install and configure web server",
    "license": "Apache-2.0",
    "min_ansible_version": "2.4",
    "galaxy_tags": ["web", "nginx"]
  }
}
```

CRITICAL REQUIREMENTS:
- Extract all metadata fields present in the file
- Preserve dependency structures including inline variables
- Return ONLY valid JSON, no explanations{% endraw %}
