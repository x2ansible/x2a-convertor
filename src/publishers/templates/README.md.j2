# {{ project_id }}

Ansible project generated by x2a-convertor.

## Roles

{% for role in roles %}
### {{ role.name }}

{% if role.description %}
{{ role.description }}
{% endif %}
{% if role.platforms %}
**Target platforms:** {{ role.platforms | join(', ') }}
{% endif %}

Run this role:

```bash
ansible-playbook playbooks/run_{{ role.name }}.yml -i inventory/hosts.yml
```
{% if role.defaults %}

**Default variables** (override in inventory or extra vars):

| Variable | Default |
|----------|---------|
{% for var_name, var_value in role.defaults.items() %}
| `{{ var_name }}` | `{{ var_value }}` |
{% endfor %}
{% endif %}

{% endfor %}
## Prerequisites

### Collections

Install required Ansible collections:

```bash
ansible-galaxy collection install -r collections/requirements.yml
```
{% if collections %}

Required collections:

{% for col in collections %}
- `{{ col.name }}`{% if col.version is defined and col.version %} ({{ col.version }}){% endif %}

{% endfor %}
{% endif %}

## Inventory

Edit `inventory/hosts.yml` to define your target hosts before running any playbook.

## Quick start

```bash
# Install required collections
ansible-galaxy collection install -r collections/requirements.yml

# Edit inventory
vi inventory/hosts.yml

# Run a playbook
ansible-playbook playbooks/run_<role_name>.yml -i inventory/hosts.yml
```
