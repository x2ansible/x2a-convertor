---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Environment-specific"
    path: "environment/%{::environment}.yaml"

  - name: "Common defaults"
    path: "common.yaml"
