# playbook-extension

Ansible playbooks must use `.yml` or `.yaml` file extensions.

## Problematic code

Files with incorrect extensions:
- `playbook.json`
- `playbook.xml`
- `playbook` (no extension)

## Correct code

Files with valid extensions:
- `playbook.yml`
- `playbook.yaml`
